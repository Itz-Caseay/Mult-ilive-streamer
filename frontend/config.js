// Edit this after you deploy the backend (Render/Railway/Fly/VPS).
// Use wss:// for a backend served over https, ws:// only for local http testing.
window.MULTICAST_CONFIG = {
  backendUrl: 'wss://multi-live-streamer-backend.onrender.com/',
};
